package util.enumeration;

public enum TableStatusEnum {
    
    FROZEN_UNOCCUPIED,
    FROZEN_ALLOCATED,
    FROZEN_OCCUPIED,
    UNFROZEN_UNOCCUPIED,
    UNFROZEN_ALLOCATED,
    UNFROZEN_OCCUPIED
    
}
